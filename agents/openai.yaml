interface:
  display_name: "Quotation JSON Generator"
  short_description: "Build quotation JSON and export matching PDFs"
  default_prompt: "Use $quotation-json-generator to create an independent import-ready quotation JSON, prefill its goods receipt when supplied, and use the configured Quotation Software app for any requested PDFs."
