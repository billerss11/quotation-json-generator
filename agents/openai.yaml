interface:
  display_name: "Quotation JSON Generator"
  short_description: "Build quotation JSON with goods-receipt drafts"
  default_prompt: "Use $quotation-json-generator to create an import-ready quotation JSON and prefill its goods receipt when supplied."
